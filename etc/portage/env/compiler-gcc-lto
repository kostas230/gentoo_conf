COMMON_FLAGS="-O3 -march=native -pipe"
CFLAGS="-flto=$(nproc) ${COMMON_FLAGS}"
CXXFLAGS="-flto=$(nproc) ${COMMON_FLAGS}"
LDFLAGS="-Wl,-O2 -Wl,--as-needed"

CC="gcc"
CXX="g++"
AR="gcc-ar"
NM="gcc-nm"
RANLIB="gcc-ranlib"
